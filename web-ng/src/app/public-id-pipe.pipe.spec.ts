import { PublicIdPipePipe } from './public-id-pipe.pipe';

describe('PublicIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PublicIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
